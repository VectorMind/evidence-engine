# Open Plans

Plan packets with work still outstanding. See each folder for details.

| Plan | Date | Status | What remains |
| --- | --- | --- | --- |
| [2026-06-06-global-routing-indexes](2026-06-06-global-routing-indexes/) | 2026-06-06 | D0 + D1 implemented; D0 representation contract closed in code; later media/semantic slices remain open. | D0/D1 implemented and tested. The D0 global-representation contract (O1–O7) is now fully implemented and tested: `summary_nodes.importance` column (catalog 0.8/8), importance summary side output with deterministic + dynamic learned priors, projection-time per-root budget enforcement with selection precedence, overflow counting and `negative_summary` rollup, the time-primary `max_build_seconds` budget with `tokens_per_sec` calibration, `representation_policy_version`, and the `doc_roundrobin_v1` rename. D2 (global semantic representative store) is now implemented: `index routing --semantic` builds a LanceDB representative store embedding `routing_payload` fresh over the same budgeted unit set, and `search text` fuses the FTS + semantic representative routes with RRF (multi-route trace). Also added: `even list` and `search text --budget low|mid|high` (Retrieval Strategy v1). D3 (media SigLIP representative routing) consolidated at spec level (Modality Asymmetry + Media representatives clauses; README two-lane diagram) and B1–B3 built: medoid selection persisted on `album_summary.attrs` reusing per-scope image proof vectors, the global SigLIP representative store (`semantic/global_representatives/siglip/{profile}.lancedb`, opt-in under `index routing --semantic`), and a fusable visual route (`_search_global_representatives_siglip`) that RRF-fuses with text routes at scope granularity. B4 added the `search text --image PATH` cross-modal probe (SigLIP visual route fused into routing at scope granularity; image hits from routed scopes returned alongside text). C1 (logical-union image recall), C2 (medoid persistence), C3 (text→media stays FTS-first) resolved; D3 B1–B4 implemented and tested (51 passing). Remaining: `media_cluster_summary` generation; the planner's `high`-budget recursive deepening once companion summaries exist; and manual real-corpus proofs (live Ollama media summaries + a real SigLIP `search text --image`). |
| [2026-06-11-repo-consolidation](2026-06-11-repo-consolidation/) | 2026-06-11 | Proposed — awaiting maintainer review. | Absorb `private-documents` + `private-media` designs into this repo, create one private knowledge base, archive both private repos. 5 open points (OP-001..OP-005) in [plan.md](2026-06-11-repo-consolidation/plan.md); OP-001 (knowledge-base location) gates Phase 3. Phases 1-4 all pending. |
